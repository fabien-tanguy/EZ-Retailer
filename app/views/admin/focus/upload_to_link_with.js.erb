<% if @photo.new_record? %>
	alert('erreur dans l\'envoi');
<% else %>
	$('#thumb-selected').html('<%= image_tag(@photo.image_url(:thumb_list), :alt => "", :class => ["center-thumb", "thumb-selected"], :id => @photo.id) %>');
	// hide the progress bar
	$('.bar').fadeOut('slow');
<% end %>