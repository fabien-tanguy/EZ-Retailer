<!DOCTYPE html>
<html>
<head>
  <title>Artisans du web | Mobile</title>
   <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag  "application", :media => "all" %>

  <%= javascript_include_tag "application" %>	

  <%= csrf_meta_tags %>
</head>
	<body class="home">
	<!-- Home -->
	<div data-role="page" class="page" <% unless request.fullpath != "/"%> id="home" <%end%>>
		<% unless request.fullpath != "/"%>
			<div id="wordmark"> <%= image_tag "logo-mobile-big.png", :alt =>" Logo Artisans du web", :width => "320" %> </div>
			<h2 id="banner">&nbsp;</h2>
		<% else %>
			<div data-role="header">
		    	<h1><%= @title %></h1>
		   </div>
		  <!-- /header -->
		<% end %>
			
		<!-- Panel -->
		<%= render('/layouts/panel') %>
		
		<div data-role="content">
			<%= render 'layouts/messages' %>
			<%= yield %>
		</div>
		<!-- end content --> 
	</div>
	<!-- end page -->	
</body>
</html>

