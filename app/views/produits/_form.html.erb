<%= simple_form_for(@produit) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :titre %>
    <%= f.input :description %>
    <%= f.input :prix %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
