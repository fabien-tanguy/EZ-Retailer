<ul data-role="listview" data-inset="true">
	<% @produits.each do |produit| %>
		<li><%= link_to image_tag(produit.image_url(:thumb_list)), produit_path(produit) %>
			<h1><%= produit.titre %></h1>
		</li>
	<% end %>
</ul>
